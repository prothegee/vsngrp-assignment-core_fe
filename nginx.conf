server {
    listen 9003;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location = /health {
        default_type application/json;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
